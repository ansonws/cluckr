<%- include("../partials/header") %>

<h2 class="my-4">Your Recent Clucks</h2>
<% if (clucks.length === 0) { %>
<h2>You don't have any clucks! Why don't you <a href="/clucks/new">CLUCK NOW</a></h2>


<% } else { for (let cluck of clucks) { %> <div class="mb-4">
        <div class="card col-6" style="min-width: 400px;">
        <div>
        <img src="/assets/fried-chicken.jpg" alt="fried chicken" height="56px" width="56px">    
        <a href="#" class="btn btn-link" id="avatar"><span><%= cluck.username %> • <%= dateConverter(cluck.createdAt) %></span><a>
        </div>
        
        <div class="card-body">
        <p class="card-text"><%= cluck.content%></p>
        </div>
        <% if (cluck.image_url) { %>
        <img class="card-img-bottom" src="<%= cluck.image_url %>" alt="<%= cluck.image_url %>"><% } %>
        </div>
    </div>
    <% } }%>

<%- include("../partials/footer") %>
